# reviews/applied/

Review files that have been applied to `data/registry.json`.

Each file here corresponds to one `python -m registry apply` execution.
Preserved as an immutable audit trail of all data changes.

Do **not** modify or delete files in this directory.
